source "https://rubygems.org"

# Local development build. GitHub Pages builds the site with its own
# toolchain, so this Gemfile is only used when you run Jekyll locally.
gem "jekyll", "~> 4.2.2"

# Needed for `jekyll serve` on Ruby 3+ (harmless on older Ruby).
gem "webrick", "~> 1.7"
