# Jekyll build output & cache
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby / Bundler
vendor/
.bundle/
Gemfile.lock

# Editor / OS
.claude/
.DS_Store

# Source resume (the generated PDF is committed, the docx is not)
*.docx

# Raw profile-photo upload (the web-sized images/profile.png is committed)
images/me.png
